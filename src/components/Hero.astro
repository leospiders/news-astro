---
import { Image } from "astro:assets";
import mobileImg from "../assets/images/image-web-3-mobile.jpg";
import desktopImg from "../assets/images/image-web-3-desktop.jpg";

interface props {
  styles?: string;
}
const { styles } = Astro.props;
---

<header class={`mb-16 ${styles}`}>
  <div class="mb-6 xl:mb-10">
  <Image class="sm:hidden" src={mobileImg} alt="image-web-3-mobile" />
  <Image
    class="hidden sm:block"
    src={desktopImg}
    alt="image-web-3-desktop"
  />
  </div>
  <div class="flex flex-col items-start xl:flex-row">
    <h1 class="text-[2.5rem]/10 font-extrabold xl:text-[3.5rem]/14 xl:flex-1">
      The Bright Future of Web 3.0?
    </h1>
    <div class="flex-1 xl:pt-2">
      <p class="text-dark-grayish-blue text-[0.9rem] mb-6">
        We dive into the next evolution of the web that claims to put the power of
        the platforms back into the hands of the people. But is it really
        fulfilling its promise?
      </p>
      <button
        class="bg-soft-red uppercase px-8 py-3 font-bold cursor-pointer hover:bg-soft-red/90 rounded-lg"
        >Read more</button
      >
    </div>
  </div>
</header>
